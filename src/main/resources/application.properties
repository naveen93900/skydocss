spring.application.name=SkyDoc

# PostgreSQL Database Configuration
#spring.datasource.url=jdbc:postgresql://localhost:5432/skydocs
#spring.datasource.username=postgres
#spring.datasource.password=N1@naveen


spring.datasource.url=${DATABASE_URL:jdbc:postgresql://YBEr7kAoa2rUnmTFqSxl4Pt6MTj5uKx6@dpg-d41kt5emcj7s7397utt0-a/skydocs}
spring.datasource.username=${DB_USERNAME:skydocs_user}
spring.datasource.password=${DB_PASSWORD:YBEr7kAoa2rUnmTFqSxl4Pt6MTj5uKx6}

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optional: Configure connection pool (HikariCP is default)
spring.datasource.hikari.maximum-pool-size=10




# ===============================
# SESSION CONFIG
# ===============================

server.port=${PORT:8080}

spring.session.jdbc.initialize-schema=always
spring.session.store-type=jdbc

# Allow circular dependencies (if needed)
spring.main.allow-circular-references=true

# Allow CORS from frontend
spring.web.resources.add-mappings=true
